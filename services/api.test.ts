import { describe, it } from 'vitest';

describe.skip('services/api Firestore helpers', () => {
  it('relies on Firestore integration tests executed in the hosting environment', () => {
    // Firestore interactions require networked integration tests which are
    // executed separately from this unit test suite.
  });
});
